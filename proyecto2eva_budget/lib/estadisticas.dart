import 'package:flutter/material.dart';

class Estadisticas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pantalla 2', style: TextStyle(fontSize: 24)),
    );
  }
}
